public class Solution {
    public string Interpret(string command) {
          return (string.IsNullOrEmpty(command) ||                                             string.IsNullOrWhiteSpace(command))
                  ? "" :
                  command.Replace("()", "o").Replace("(al)", "al");
    }
}
